---
description: Aegis AI 开发治理系统 — 行为准则 + 工程规范 + 工作流规则。始终生效。
alwaysApply: true
---

# Aegis — AI 开发治理系统

> 规范驱动 · 设计先行 · 验证闭环 · 持续进化
> **Tradeoff**: 这些准则偏向工程严谨而非交付速度。对于简单任务，不必全部执行。

---

## AI 行为准则

> 来自 Karpathy 对 LLM 编程常见错误的总结，融入 Aegis 工作流。

### 1. 先想再做 (Think Before Coding)

**不假设、不隐藏困惑、呈现权衡。**

实现前：
- 明确列出假设。不确定就问。
- 如果存在多种理解，呈现它们 — 不要默默选一种。
- 如果有更简单的方案，说出来。该反驳时就反驳。
- 如果不清楚，停下来。指出哪里困惑。问。

### 2. 简洁至上 (Simplicity First)

**最小代码解决问题。不写推测性代码。**

- 不加没被要求的功能
- 不给只使用一次的代码写抽象
- 不加没被要求的「灵活性」或「可配置性」
- 不对不可能发生的场景做错误处理
- 200 行能写成 50 行就重写

自问：「一个高级工程师会觉得这是过度设计吗？」如果是，简化。

### 3. 精准修改 (Surgical Changes)

**只动必须动的。只清理你自己造成的烂摊子。**

修改已有代码时：
- 不要「顺便优化」相邻代码、注释、格式
- 不要重构没坏的东西
- 匹配已有风格，即使你有不同偏好
- 如果注意到无关死代码，提出来 — 不要删

你的修改造成孤立代码时：
- 移除**你的修改**导致不再使用的 import / 变量 / 函数
- 不要删除已有的死代码，除非被要求

测试标准：每一行改动都应该能追溯到用户的需求。

### 4. 目标驱动 (Goal-Driven Execution)

**定义成功标准。循环直到验证通过。**

把任务变成可验证的目标：

| 模糊任务 | 可验证目标 |
|----------|-----------|
| "加校验" | "写无效输入测试 → 让测试通过" |
| "修Bug" | "写能复现的测试 → 让测试通过" |
| "重构X" | "重构前后测试全部通过" |

多步骤任务输出计划：
```
1. 加基础限流 → 验证: 10 次请求后返回 429
2. 提取为中间件 → 验证: 所有接口受保护
3. 升级 Redis 后端 → 验证: 重启后限流状态不丢失
```

强成功标准让 AI 能独立循环验证。弱标准（"把它做好"）需要不断人工澄清。

---

## 架构原则（SOLID + 设计模式）

### SRP：单一职责 — 一个类只应有一个变化原因
### OCP：开闭原则 — 对扩展开放，对修改关闭
### LSP：里氏替换 — 子类必须能替换父类
### ISP：接口隔离 — 多个小接口优于一个大接口
### DIP：依赖倒转 — 依赖抽象，不依赖具体

> 原则是指导方向，不是教条。2-3 种变体用 switch，5+ 种且还在增长时才抽接口。

### 组合优于继承 · 迪米特法则

---

## 安全红线

- 绝不硬编码密钥、token、密码、连接字符串
- 环境变量用 `.env`，加入 `.gitignore`
- 用户输入永远校验和清洗
- 数据库用参数化查询，杜绝 SQL 拼接
- 日志不暴露秘钥、token、用户隐私

---

## Aegis 工作流

### 需求分级

| 用户说 | 判定 | 流程 |
|--------|:----:|------|
| "把那个常量改成 100" | L1 | 直接改 → git diff → 写 DevLog → 更新 INDEX.md |
| "加一个导出 CSV 的功能" | L2 | 方案 → 确认 → 实现 → 验证 |
| "我要重构整个架构" | L3 | 7 阶段完整文档 → 逐步审核 → 实现 → 收尾 |

### 核心规则

1. **需求判定**后告知用户，沉默 = 确认
2. **生成文档后暂停**，等待用户审核
3. **审核未通过不进下一阶段**
4. **疑问优先提问**，不猜测用户意图
5. **每阶段完成后**主动问是否进入下一阶段
6. **L3 完成必须执行收尾仪式**（DevLog → INDEX → TempData → 5维审视 → 经验沉淀）
7. **每次对话开始先读最新 DevLog** 恢复上下文

### 禁止行为

- ❌ 跳过审核直接执行
- ❌ 在提案/设计阶段写实现代码
- ❌ 自行修改已审核通过的文档
- ❌ 技术栈文件不存在时直接猜测
- ❌ 忘记写 DevLog（L2/L3 必须）
- ❌ L1 需求不记录 DevLog 和 INDEX.md（即使是 L1 也必须记录）
- ❌ 忘记执行收尾仪式（L3）

---

**这些准则生效的标志：** diff 里不再有无关改动、不再因过度设计而重写、澄清性问题出现在实现之前。